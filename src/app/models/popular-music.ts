export interface PopularArtist {
  id?: string;
  name: string;
  sortName: string;
}

export interface PopularAlbum {
  id?: string;
  title: string;
  artist: string;
  sortTitle: string;
}

export interface PopularSong {
  id?: string;
  title: string;
  artist: string;
  sortTitle: string;
}
